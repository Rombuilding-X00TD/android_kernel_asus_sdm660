clone: 
  depth: 1
kind: pipeline
name: cooking
steps: 
  - 
    commands: 
      - "curl -s https://raw.githubusercontent.com/Rombuilding-X00TD/kernel_build_script-ci/main/build.sh | bash"
    environment: 
      TELEGRAM_TOKEN: 
        from_secret: BOT_API_KEY
    image: xealea/xea:01
    name: Xea-Images
    trigger: 
      branch: 
          - los
          - stock
